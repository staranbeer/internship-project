// // store an array of names in local storage
// import { useState } from "react";

// export const useLocalStorage = (key: string, initialValue = []) => {
// //  use a state hook to store the value of the key in local storage

//   return [likedPokemon, setLikedPokemon];
// };
